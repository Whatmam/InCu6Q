1. 빌드루트 개발환경 만들기
Create_buildroot.sh


2. 새로 빌드된 이미지(buildroot/output/image) 사용 할 경우
 ./make_rootfs.sh clean
 ./make_rootfs.sh copy
 ./make_rootfs.sh

 3. Filesystem에 저장된 이미지(rootfs_files/rootfs_base) 사용 할 경우
 ./make_rootfs.sh